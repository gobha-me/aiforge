# Coordinated Venice/HTTP contract failures (slice193)

Save before implementation. Pure configure/link and hermetic loopback fixtures;
no provider or Kubernetes calls, source advertisement or new resolver lifetime.

- Installed minimum0.29.18 is insufficient without exact capability marker;
  missing canonical target, missing marker, fake claimed version/header,
  incompatible caller-preexisting HTTP target and stale sibling all refuse.
- Actual one canonical HTTP target must be unsplit header-only0.51.x,
  OpenSSL>=3 only, exceptions enabled, synchronous resolver and canonical
  header-count/line/maximum-line macros. Test missing APIs/transitive links,
  TLS-off/alternative-TLS, old headers, compiled target and altered macros.
- Aliases and nested link-expression aliases resolve to actual chosen targets;
  no added fallback after incompatible target selection. Caller options survive
  preexisting and installed reuse. Source fallback embeds no tests/tools/install.
- Check actual header/API capabilities every configure: a valid build tree
  reconfigured with changed header/macros must fail, not reuse old cached proof.
- Negative package fixtures isolate roots/registries and poison source fallback;
  separately permit a compatible installed alternative after an obsolete hint.
  Imported/embedded expectations are asserted, not inferred from successful build.
- Actual fetched, sibling, preexisting and installed Venice client compiles/links
  on GCC+Clang including c-ares/httplib/JSON/OpenSSL/Threads closure. Installed
  prefix uses real dependency exports; seed c-ares source without duplicate recipe.
  Source Venice plus installed dependencies must expose the canonical HTTP
  target to the parent gate and restore different normal/cache caller preferences.
- Existing80/89/90 localhost fixtures retain protocol behavior. Audio transcription
  multipart uploaded bytes/filename/content-type are read from form.files while
  text metadata stays separate in form.fields. No weakening old assertions.
- Full dependency scripts retain182/183/188 probes and adapter-free nonactivation.
  Source/installed consumer builds are serialized at parallel2 with root approval.
